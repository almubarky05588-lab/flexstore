name: Build iOS

on:
  push:
    branches: [main]
    paths-ignore:
      - 'supabase/**'
      - 'README.md'
  workflow_dispatch:

jobs:
  ios:
    runs-on: macos-latest
    timeout-minutes: 60
    env:
      BUNDLE_ID: com.flexstore.flexstore
      TEAM_ID: ${{ secrets.TEAM_ID }}
      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
      FASTLANE_SKIP_UPDATE_CHECK: 1
      FASTLANE_HIDE_CHANGELOG: 1

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate iOS project
        run: |
          mkdir -p /tmp/keep
          cp -r lib assets pubspec.yaml /tmp/keep/
          flutter create --platforms=ios --org com.flexstore --project-name flexstore .
          rm -rf lib assets pubspec.yaml test
          cp -r /tmp/keep/lib /tmp/keep/assets .
          cp /tmp/keep/pubspec.yaml .

      - name: Patch Info.plist
        run: |
          PL=ios/Runner/Info.plist
          /usr/libexec/PlistBuddy -c "Add :ITSAppUsesNonExemptEncryption bool false" "$PL" \
            || /usr/libexec/PlistBuddy -c "Set :ITSAppUsesNonExemptEncryption false" "$PL"
          /usr/libexec/PlistBuddy -c "Add :CFBundleLocalizations array" "$PL" || true
          /usr/libexec/PlistBuddy -c "Add :CFBundleLocalizations:0 string ar" "$PL" || true
          /usr/libexec/PlistBuddy -c "Add :CFBundleDevelopmentRegion string ar" "$PL" \
            || /usr/libexec/PlistBuddy -c "Set :CFBundleDevelopmentRegion ar" "$PL"
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryUsageDescription string 'photo access'" "$PL" \
            || /usr/libexec/PlistBuddy -c "Set :NSPhotoLibraryUsageDescription 'photo access'" "$PL"

      - run: flutter pub get

      - name: Set build number
        run: echo "BUILD_NUMBER=$(( ${{ github.run_number }} + 1 ))" >> $GITHUB_ENV

      - name: Build unsigned
        run: |
          flutter build ios --release --no-codesign \
            --build-number=${{ env.BUILD_NUMBER }} \
            --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} \
            --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}

      - name: Write API key
        env:
          ASC_KEY_P8: ${{ secrets.ASC_KEY_P8 }}
        run: |
          mkdir -p ios/keys
          printf '%s\n' "$ASC_KEY_P8" > ios/keys/AuthKey.p8

      - name: Create Fastfile
        run: |
          mkdir -p ios/fastlane
          cat > ios/fastlane/Fastfile <<'RUBY'
          default_platform(:ios)

          platform :ios do
            lane :beta do
              key_path = File.expand_path("../keys/AuthKey.p8", __dir__)

              api_key = app_store_connect_api_key(
                key_id: ENV["ASC_KEY_ID"],
                issuer_id: ENV["ASC_ISSUER_ID"],
                key_filepath: key_path,
                in_house: false
              )

              create_keychain(
                name: "build.keychain",
                password: "temp1234",
                default_keychain: true,
                unlock: true,
                timeout: 3600,
                lock_when_sleeps: false
              )

              # لا ننشئ شهادة جديدة (السلوت ممتلئ ولا يجوز حذف شهادة قد تخص تطبيقًا آخر).
              # نعيد استخدام إحدى شهادات التوزيع الموجودة عبر تنزيلها من آبل.
              require 'spaceship'
              Spaceship::ConnectAPI.token = Spaceship::ConnectAPI::Token.create(
                key_id: ENV["ASC_KEY_ID"],
                issuer_id: ENV["ASC_ISSUER_ID"],
                filepath: key_path
              )

              keychain_path = File.expand_path("~/Library/Keychains/build.keychain-db")

              dist_certs = Spaceship::ConnectAPI::Certificate.all.select do |c|
                ["IOS_DISTRIBUTION", "DISTRIBUTION"].include?(c.certificate_type.to_s)
              end
              UI.important("distribution certs found: #{dist_certs.count}")
              UI.user_error!("no distribution certificate found on the account") if dist_certs.empty?

              chosen = dist_certs.first
              cert_content = chosen.certificate_content
              cer_path = File.expand_path("dist.cer", Dir.pwd)
              File.write(cer_path, Base64.decode64(cert_content))
              sh("security import #{cer_path} -k #{keychain_path} -T /usr/bin/codesign -A || true")
              UI.important("imported distribution certificate: #{chosen.id}")

              sigh(
                app_identifier: ENV["BUNDLE_ID"],
                api_key: api_key,
                team_id: ENV["TEAM_ID"],
                development: false,
                force: true
              )

              update_code_signing_settings(
                use_automatic_signing: false,
                path: "Runner.xcodeproj",
                team_id: ENV["TEAM_ID"],
                bundle_identifier: ENV["BUNDLE_ID"],
                code_sign_identity: "Apple Distribution",
                profile_name: lane_context[SharedValues::SIGH_NAME]
              )

              build_app(
                workspace: "Runner.xcworkspace",
                scheme: "Runner",
                configuration: "Release",
                export_method: "app-store",
                skip_profile_detection: true,
                export_options: {
                  provisioningProfiles: {
                    ENV["BUNDLE_ID"] => lane_context[SharedValues::SIGH_NAME]
                  }
                }
              )

              upload_to_testflight(
                api_key: api_key,
                skip_waiting_for_build_processing: true
              )
            end
          end
          RUBY

      - name: Run fastlane
        working-directory: ios
        run: |
          gem install fastlane --no-document
          fastlane beta

      - name: Done
        run: echo "uploaded to TestFlight"
